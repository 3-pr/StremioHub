const I18N = {
  ar: {
    app_title: "StremioHub",
    app_subtitle: "All-in-one",
    email_label: "البريد الإلكتروني",
    email_placeholder: "example@email.com",
    password_label: "كلمة المرور",
    password_placeholder: "••••••••",
    login_btn: "تسجيل الدخول",
    settings_title: "الإعدادات",
    account_title: "حساب Stremio",
    loading: "جاري التحميل...",
    logout_btn: "تسجيل الخروج",
    extension_settings: "إعدادات الإضافة",
    lang_title: "اللغة / Language",
    lang_desc: "تغيير لغة عرض الواجهة.",
    autosave_title: "الحفظ التلقائي في المواقع",
    autosave_desc: "حفظ العمل مباشرة في مكتبتك عند الضغط على الزر في المواقع الخارجية، دون عرض نافذة التفاصيل أولاً.",
    sort_title: "خيارات الترتيب في المكتبة",
    sort_desc: "عرض شريط ترتيب وتصفية الأعمال (آخر مشاهدة، أبجدي، شُوهد/لم يشاهد) أعلى مكتبتك.",
    open_method_title: "طريقة الفتح في Stremio",
    open_method_desc: 'كيف تفضل أن يتم فتح الأعمال عند اختيار "فتح"؟',
    open_web: "Stremio Web (المتصفح)",
    open_app: "تطبيق Stremio",
    detail_style_title: "شكل نافذة التفاصيل",
    detail_style_desc: "كيف تفضل عرض تفاصيل الأعمال (في شاشة كاملة أم بطاقة عائمة)؟",
    style_fullscreen: "شاشة كاملة",
    style_card: "بطاقة عائمة",
    meta_addon_title: "مصدر وصف العمل",
    meta_addon_desc: "اختر الإضافة المفضلة لديك لجلب تفاصيل الأعمال.",
    meta_addon_auto: "الدمج الذكي (Auto)",
    size_title: "حجم الواجهة",
    size_desc: "تخصيص أبعاد نافذة الإضافة لتناسب شاشتك المفتوحة.",
    size_default: "الافتراضي (عريض)",
    size_compact: "مدمج (أصغر)",
    sites_title: "المواقع المدعومة",
    sites_desc: "تحكم بظهور الأزرار في كل موقع وماذا تفعل (حفظ أو فتح).",
    action_save: "حفظ",
    action_open: "فتح",
    refresh_btn: "تحديث",
    search_btn: "بحث",
    settings_btn: "الإعدادات",
    search_placeholder: "ابحث عن فيلم أو مسلسل...",
    no_library_msg: "لا توجد أعمال في المكتبة. ابحث وأضف بعض الأعمال!",
    no_results_msg: "لا توجد نتائج مطابقة.",
    sort_last_watched: "آخر مشاهدة",
    sort_alphabetical: "A-Z",
    filter_all: "الكل",
    filter_watched: "شُوهد",
    filter_unwatched: "لم يشاهد",
    search_movies: "بحث عن فيلم",
    search_series: "بحث عن مسلسل",
    see_all: "عرض الكل",
    save_btn: "حفظ في المكتبة",
    open_btn: "فتح في Stremio",
    saving: "جاري الحفظ...",
    saved: "تم الحفظ ✓",
    opening: "جاري الفتح...",
    opened: "تم الفتح ✓",
    error: "حدث خطأ",
    tab_movies: "الأفلام",
    tab_series: "المسلسلات",
    tab_continue: "قيد المشاهدة",
    tab_general: "عام",
    tab_library: "المكتبة",
    tab_websites: "المواقع",
    sort_alphabetical_reverse: "Z-A",
    filter_placeholder: "تصفية...",
    story_title: "القصة",
    translate_btn: "ترجمة",
    translate_btn_title: "ترجمة الوصف",
    cast_title: "طاقم العمل",
    watch_now: "مشاهدة الآن",
    app_btn: "التطبيق",
    add_lib: "إضافة للمكتبة",
    remove_lib: "إزالة من المكتبة",
    mark_watched: "تحديد كمشاهد",
    marked_watched: "شُوهد",
    back_btn: "رجوع",
    translate_loading: "جاري...",
    translate_done: "تمت",
    translate_error: "خطأ",
    translate_revert: "الأصلي",

    msg_enter_credentials: "يرجى إدخال البريد الإلكتروني وكلمة المرور",
    msg_invalid_credentials: "البريد الإلكتروني أو كلمة المرور غير صحيحة",
    msg_network_error: "خطأ في الاتصال. تحقق من الإنترنت.",
    msg_logged_out: "تم تسجيل الخروج",
    msg_updated: "تم التحديث ✓",
    msg_session_expired: "انتهت الجلسة — سجل دخولك مرة أخرى",
    msg_library_error: "خطأ في جلب المكتبة",
    msg_added_library: "تمت الإضافة للمكتبة ✓",
    msg_add_failed: "فشلت الإضافة",
    msg_marked_watched: "تم التسجيل كمُشاهد ✓",
    msg_removed_watched: "تم إزالة المشاهدة",
    msg_removed_library: "تم الحذف من المكتبة",
    msg_remove_failed: "خطأ في الحذف",
    msg_error: "خطأ",
    dev_credits: "صُنع من مُشاهد، إلى كل المشاهدين 🍿",
    stat_watched: "شُوهدت",
    stat_addons: "إضافات",
    web_btn: "المتصفح",
    empty_movies: "مكتبة الأفلام فارغة",
    empty_series: "لا توجد مسلسلات في مكتبتك",
    empty_continue: "لا يوجد شيء قيد المشاهدة",
    next_episode: "الحلقة التالية: S{s}E{e}",
    resume_episode: "استكمال S{s}E{e}",
    new_episodes_count: "لديك {count} حلقات جديدة",
    episodes_title: "الحلقات",
    season_n: "الموسم {n}",
    play_episode: "شاهد S{s}E{e} الآن",
    mark_read_title: "تحديد كمقروء",
    unmark_read_title: "إلغاء المشاهدة",
    mark_season_watched: "مشاهدة الموسم",
    unmark_season_watched: "إلغاء مشاهدة الموسم",
    coming_soon: "قريباً",
    support_url: "https://creators.sa/i",
    support_text: "أعجبتك الإضافة؟",
    toast_enabled_title: "إشعار الحفظ في المكتبة",
    toast_enabled_desc: "عرض إشعار صغير في زاوية الشاشة للتأكيد عند حفظ عمل جديد في مكتبتك.",
    toast_position_title: "موقع إشعار الحفظ",
    toast_position_desc: "اختر في أي زاوية من الشاشة يظهر إشعار الحفظ.",
    pos_top_right: "فوق يمين",
    pos_top_left: "فوق يسار",
    pos_bottom_right: "تحت يمين",
    pos_bottom_left: "تحت يسار"
  },
  en: {
    app_title: "StremioHub",
    app_subtitle: "All-in-one",
    email_label: "Email Address",
    email_placeholder: "example@email.com",
    password_label: "Password",
    password_placeholder: "••••••••",
    login_btn: "Login",
    settings_title: "Settings",
    account_title: "Stremio Account",
    loading: "Loading...",
    logout_btn: "Logout",
    extension_settings: "Extension Settings",
    lang_title: "Language / اللغة",
    lang_desc: "Change extension interface language.",
    autosave_title: "Auto-save on Sites",
    autosave_desc: "Save items directly to your library when clicking buttons on external sites, without showing details.",
    sort_title: "Library Sorting",
    sort_desc: "Show a sorting and filtering bar at the top of your library.",
    open_method_title: "Open Method",
    open_method_desc: "How would you prefer to open items when choosing 'Open'?",
    open_web: "Stremio Web (Browser)",
    open_app: "Stremio App",
    detail_style_title: "Details View Style",
    detail_style_desc: "Choose how movie and series details appear (as a full screen or a pop-up card).",
    style_fullscreen: "Full Screen",
    style_card: "Pop-up Card",
    meta_addon_title: "Metadata Source",
    meta_addon_desc: "Choose your preferred addon for fetching item details.",
    meta_addon_auto: "Smart Merge (Auto)",
    size_title: "Popup Size",
    size_desc: "Customize the extension window dimensions.",
    size_default: "Default (Wide)",
    size_compact: "Compact (Smaller)",
    sites_title: "Supported Sites",
    sites_desc: "Control button visibility and action (Save or Open).",
    action_save: "Save",
    action_open: "Open",
    refresh_btn: "Refresh",
    search_btn: "Search",
    settings_btn: "Settings",
    search_placeholder: "Search for a movie or series...",
    no_library_msg: "No items in your library. Search and add some!",
    no_results_msg: "No matching results.",
    sort_last_watched: "Last Watched",
    sort_alphabetical: "A-Z",
    filter_all: "All",
    filter_watched: "Watched",
    filter_unwatched: "Unwatched",
    search_movies: "Movies Search",
    search_series: "Series Search",
    see_all: "See All",
    save_btn: "Save to Library",
    open_btn: "Open in Stremio",
    saving: "Saving...",
    saved: "Saved ✓",
    opening: "Opening...",
    opened: "Opened ✓",
    error: "Error",
    tab_movies: "Movies",
    tab_series: "Series",
    tab_continue: "Continue",
    tab_general: "General",
    tab_library: "Library",
    tab_websites: "Websites",
    sort_alphabetical_reverse: "Z-A",
    filter_placeholder: "Filter...",
    story_title: "Story",
    translate_btn: "Translate",
    translate_btn_title: "Translate description",
    cast_title: "Cast",
    watch_now: "Watch Now",
    app_btn: "App",
    add_lib: "Add to Library",
    remove_lib: "Remove from Library",
    mark_watched: "Mark as Watched",
    marked_watched: "Watched",
    back_btn: "Back",
    translate_loading: "Loading...",
    translate_done: "Done",
    translate_error: "Error",
    translate_revert: "Original",

    msg_enter_credentials: "Please enter email and password",
    msg_invalid_credentials: "Email or password incorrect",
    msg_network_error: "Connection error. Check your internet.",
    msg_logged_out: "Logged out",
    msg_updated: "Updated ✓",
    msg_session_expired: "Session expired — please log in again",
    msg_library_error: "Error fetching library",
    msg_added_library: "Added to Library ✓",
    msg_add_failed: "Failed to add",
    msg_marked_watched: "Marked as Watched ✓",
    msg_removed_watched: "Watch status removed",
    msg_removed_library: "Removed from Library",
    msg_remove_failed: "Failed to remove",
    msg_error: "Error",
    dev_credits: "Made by a viewer, for the viewers 🍿",
    stat_watched: "Watched",
    stat_addons: "Add-ons",
    web_btn: "Web",
    empty_movies: "Movie library is empty",
    empty_series: "No series in your library",
    empty_continue: "Nothing is currently being watched",
    next_episode: "Next: S{s}E{e}",
    resume_episode: "Resume S{s}E{e}",
    new_episodes_count: "{count} new episodes",
    episodes_title: "Episodes",
    season_n: "Season {n}",
    play_episode: "Watch S{s}E{e} Now",
    mark_read_title: "Mark as read",
    unmark_read_title: "Remove from history",
    mark_season_watched: "Watch Season",
    unmark_season_watched: "Unwatch Season",
    coming_soon: "Coming Soon",
    support_url: "https://ko-fi.com/i0zzw",
    support_text: "Support me on Ko-fi",
    toast_enabled_title: "Library Save Notification",
    toast_enabled_desc: "Show a small notification in the corner when successfully saving an item.",
    toast_position_title: "Notification Position",
    toast_position_desc: "Choose the corner of the screen where the notification will appear.",
    pos_top_right: "Top Right",
    pos_top_left: "Top Left",
    pos_bottom_right: "Bottom Right",
    pos_bottom_left: "Bottom Left"
  }
};

function applyI18N(lang) {
  const t = I18N[lang] || I18N.ar;
  document.documentElement.lang = lang;
  document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';

  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    if (t[key]) el.textContent = t[key];
  });

  document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
    const key = el.getAttribute('data-i18n-placeholder');
    if (t[key]) el.placeholder = t[key];
  });

  document.querySelectorAll('[data-i18n-title]').forEach(el => {
    const key = el.getAttribute('data-i18n-title');
    if (t[key]) el.title = t[key];
  });

  const btnTranslate = document.getElementById('btn-translate');
  if (btnTranslate) {
    btnTranslate.style.display = lang === 'en' ? 'none' : '';
  }

  const supportHeaderBtn = document.getElementById('support-header-btn');
  if (supportHeaderBtn) {
    supportHeaderBtn.href = t.support_url || "https://creators.sa/i";
    supportHeaderBtn.title = t.support_text || "أعجبتك الإضافة؟";
  }

  const supportFooterBtn = document.getElementById('support-footer-btn');
  if (supportFooterBtn) {
    supportFooterBtn.href = t.support_url || "https://creators.sa/i";
    const svgIcon = supportFooterBtn.innerHTML.match(/<svg.*?<\/svg>/s)?.[0] || '';
    supportFooterBtn.innerHTML = svgIcon + ' ' + (t.support_text || "أعجبتك الإضافة؟");
  }
}

// popup.js — StremioHub Main Controller (Self-contained)

// ==================== Global Image Error Handler (CSP Safe) ====================
document.addEventListener('error', (e) => {
  if (e.target.tagName && e.target.tagName.toLowerCase() === 'img') {
    e.target.style.display = 'none';
  }
}, true);

// ==================== Stremio API ====================
const BASE_URL = 'https://api.strem.io/api';
const CINEMETA_BASE = 'https://v3-cinemeta.strem.io';

const StremioAPI = {
  async login(email, password) {
    const res = await fetch(`${BASE_URL}/login`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email, password, FacebookTkn: '' })
    });
    const data = await res.json();
    if (data.error) {
      const errMsg = typeof data.error === 'string' ? data.error : (data.error.message || 'Login failed');
      throw new Error(errMsg);
    }
    if (data.result?.authKey) {
      return {
        authKey: data.result.authKey,
        email: data.result.email || email,
        userId: data.result._id || data.result.id
      };
    }
    return null;
  },

  async getLibrary(authKey) {
    const res = await fetch(`${BASE_URL}/datastoreGet`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({ authKey, collection: 'libraryItem', ids: [], all: true })
    });
    const data = await res.json();
    if (data.error) throw new Error(data.error);
    return data.result || [];
  },

  async getAddons(authKey) {
    const res = await fetch(`${BASE_URL}/addonCollectionGet`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({ authKey, update: true })
    });
    const data = await res.json();
    if (data.error) throw new Error(data.error);
    return data.result?.addons || [];
  },

  async addToLibrary(authKey, itemMeta) {
    const now = new Date().toISOString();
    const imdbId = itemMeta.imdb_id || itemMeta.id || itemMeta._id;
    const type = itemMeta.type || 'movie';
    const name = itemMeta.name || '';

    const libraryItem = {
      _id: imdbId,
      id: imdbId,
      imdb_id: imdbId,
      name: name,
      type: type,
      poster: itemMeta.poster || '',
      posterShape: 'poster',
      background: itemMeta.background || '',
      logo: itemMeta.logo || '',
      description: itemMeta.description || '',
      releaseInfo: String(itemMeta.releaseInfo || itemMeta.year || ''),
      year: String(itemMeta.year || itemMeta.releaseInfo || ''),
      runtime: String(itemMeta.runtime || ''),
      imdbRating: String(itemMeta.imdbRating || ''),
      genres: Array.isArray(itemMeta.genres) ? itemMeta.genres : (Array.isArray(itemMeta.genre) ? itemMeta.genre : []),
      director: Array.isArray(itemMeta.director) ? itemMeta.director : [],
      cast: Array.isArray(itemMeta.cast) ? itemMeta.cast : [],
      released: itemMeta.released ? String(itemMeta.released) : now,
      slug: itemMeta.slug || `${type}/${name.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-${imdbId}`,
      behaviorHints: {
        defaultVideoId: type === 'movie' ? imdbId : (itemMeta.behaviorHints && itemMeta.behaviorHints.defaultVideoId ? String(itemMeta.behaviorHints.defaultVideoId) : null),
        hasScheduledVideos: itemMeta.behaviorHints ? !!itemMeta.behaviorHints.hasScheduledVideos : false,
      },
      removed: false,
      temp: false,
      _ctime: itemMeta._ctime || now,
      _mtime: itemMeta._mtime || now,
      state: {
        lastWatched: now,
        timeWatched: 0,
        timeOffset: 0,
        overallTimeWatched: 0,
        timesWatched: 0,
        flaggedWatched: 0,
        duration: 0,
        video_id: type === 'movie' ? imdbId : null,
        watched: null,
        noNotif: false,
      }
    };

    if (libraryItem.type === 'movie') {
      libraryItem.videos = [];
    }

    const res = await fetch(`${BASE_URL}/datastorePut`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({ authKey, collection: 'libraryItem', changes: [libraryItem] })
    });
    const data = await res.json();
    if (data.error) {
      console.error('addToLibrary API Error:', data.error);
      throw new Error(data.error.message || 'API Error');
    }
    return true;
  },

  async markAsWatched(authKey, libraryItem) {
    const currentState = libraryItem.state || {};
    const isWatched = (currentState.timesWatched || 0) > 0;
    const newTimesWatched = isWatched ? 0 : 1;

    const payloadItem = {
      ...libraryItem,
      _mtime: new Date().toISOString(),
      state: {
        ...currentState,
        lastWatched: new Date().toISOString(),
        timesWatched: newTimesWatched,
        watched: null
      }
    };

    const res = await fetch(`${BASE_URL}/datastorePut`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({
        authKey, collection: 'libraryItem',
        changes: [payloadItem]
      })
    });
    return { ok: res.ok, isWatched: newTimesWatched > 0 };
  },

  async removeFromLibrary(authKey, libraryItem) {
    const payloadItem = {
      ...libraryItem,
      removed: true,
      removedAt: new Date().toISOString(),
      _mtime: new Date().toISOString()
    };

    if (payloadItem.releaseInfo) payloadItem.releaseInfo = String(payloadItem.releaseInfo);
    if (payloadItem.year) payloadItem.year = String(payloadItem.year);
    if (!Array.isArray(payloadItem.genres)) payloadItem.genres = [];
    if (!Array.isArray(payloadItem.cast)) payloadItem.cast = [];

    const res = await fetch(`${BASE_URL}/datastorePut`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({
        authKey, collection: 'libraryItem',
        changes: [payloadItem]
      })
    });

    const data = await res.json();
    if (data.error) throw new Error(data.error.message);
    return true;
  },

  async clearProgress(authKey, libraryItem) {
    const payloadItem = {
      ...libraryItem,
      _mtime: new Date().toISOString(),
      state: {
        ...(libraryItem.state || {}),
        timeOffset: 0,
        timeWatched: 0,
        overallTimeWatched: 0,
        video_id: null,
        lastWatched: new Date().toISOString()
      }
    };

    if (libraryItem.temp) {
      payloadItem.removed = true;
      payloadItem.temp = false;
    }

    const res = await fetch(`${BASE_URL}/datastorePut`, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
      body: JSON.stringify({
        authKey, collection: 'libraryItem',
        changes: [payloadItem]
      })
    });
    return res.ok;
  }
};

// ==================== Universal Addon Engine ====================
const SearchEngine = {
  async search(query) {
    const encoded = encodeURIComponent(query);
    const urlsToFetch = [];

    if (!state.addons || state.addons.length === 0) {
      urlsToFetch.push(`${CINEMETA_BASE}/catalog/movie/top/search=${encoded}.json`);
      urlsToFetch.push(`${CINEMETA_BASE}/catalog/series/top/search=${encoded}.json`);
    } else {
      state.addons.forEach(addon => {
        if (!addon.manifest || !addon.manifest.catalogs) return;
        addon.manifest.catalogs.forEach(cat => {
          let supportsSearch = false;
          if (cat.extra) {
            supportsSearch = cat.extra.some(e => e.name === 'search');
          } else if (cat.extraSupported) {
            supportsSearch = cat.extraSupported.includes('search');
          }

          if (supportsSearch && (cat.type === 'movie' || cat.type === 'series')) {
            const baseUrl = addon.transportUrl.replace('/manifest.json', '');
            urlsToFetch.push(`${baseUrl}/catalog/${cat.type}/${cat.id}/search=${encoded}.json`);
          }
        });
      });
    }

    const responses = await Promise.allSettled(urlsToFetch.map(url => fetch(url).then(r => r.json())));

    let allMetas = [];
    responses.forEach(res => {
      if (res.status === 'fulfilled' && res.value && res.value.metas) {
        allMetas.push(...res.value.metas);
      }
    });

    const seen = new Set();
    const uniqueMetas = [];
    allMetas.forEach(meta => {
      const id = meta.imdb_id || meta.id;
      if (!seen.has(id)) {
        seen.add(id);
        uniqueMetas.push(meta);
      }
    });

    return uniqueMetas;
  },

  async getMeta(type, imdbId) {
    const urlsToFetch = [];

    if (state.metaAddon && state.metaAddon !== 'auto') {
      urlsToFetch.push(`${state.metaAddon}/meta/${type}/${imdbId}.json`);
    } else if (!state.addons || state.addons.length === 0) {
      urlsToFetch.push(`${CINEMETA_BASE}/meta/${type}/${imdbId}.json`);
    } else {
      state.addons.forEach(addon => {
        if (!addon.manifest || !addon.manifest.resources) return;
        let supportsMeta = false;

        if (Array.isArray(addon.manifest.resources)) {
          addon.manifest.resources.forEach(res => {
            if (typeof res === 'string' && res === 'meta') {
              if (!addon.manifest.types || addon.manifest.types.includes(type)) supportsMeta = true;
            } else if (typeof res === 'object' && res.name === 'meta') {
              if (!res.types || res.types.includes(type)) supportsMeta = true;
            }
          });
        }

        if (supportsMeta) {
          const baseUrl = addon.transportUrl.replace('/manifest.json', '');
          urlsToFetch.push(`${baseUrl}/meta/${type}/${imdbId}.json`);
        }
      });
    }

    if (!urlsToFetch.some(url => url.includes(CINEMETA_BASE))) {
      urlsToFetch.push(`${CINEMETA_BASE}/meta/${type}/${imdbId}.json`);
    }

    const responses = await Promise.allSettled(urlsToFetch.map(url => fetch(url).then(r => r.json())));

    let bestMeta = null;

    // Find Cinemeta fallback which is the last item in urlsToFetch (or anywhere it matched)
    let cinemetaMeta = null;
    responses.forEach((res, i) => {
      if (urlsToFetch[i].includes(CINEMETA_BASE) && res.status === 'fulfilled' && res.value?.meta) {
        cinemetaMeta = res.value.meta;
      }
    });

    // If user selected an addon (or we auto-selected the first one), try to get it
    if (state.metaAddon && state.metaAddon !== 'auto') {
      const selectedRes = responses[0];
      let selectedMeta = null;
      if (selectedRes.status === 'fulfilled' && selectedRes.value?.meta) {
        selectedMeta = selectedRes.value.meta;
      }

      if (cinemetaMeta && selectedMeta) {
        bestMeta = { ...cinemetaMeta, ...selectedMeta };
        // Preserve arrays/fields if the selected addon omitted them
        if (!selectedMeta.cast || selectedMeta.cast.length === 0) bestMeta.cast = cinemetaMeta.cast;
        if (!selectedMeta.director || selectedMeta.director.length === 0) bestMeta.director = cinemetaMeta.director;
        if (!selectedMeta.description) bestMeta.description = cinemetaMeta.description;
      } else {
        bestMeta = selectedMeta || cinemetaMeta;
      }
    } else {
      bestMeta = cinemetaMeta;
    }

    if (!bestMeta) throw new Error('Meta not found across addons');
    return bestMeta;
  }
};

function isWatchedItem(item) {
  if (!item || !item.state) return false;
  const s = item.state;
  if ((s.timesWatched || 0) > 0 || (s.flaggedWatched || 0) > 0 || s.watched === true || s.watched === 1) return true;
  if (s.timeOffset && s.duration) {
    if (s.timeOffset / s.duration > 0.95) return true;
  }
  return false;
}

// ==================== Library Cache ====================
const CACHE_DURATION = 5 * 60 * 1000;

const Library = {
  async get(authKey, forceRefresh = false, sortType = 'lastwatched', filterType = 'all') {
    const cached = await chrome.storage.local.get(['library_cache']);
    const cache = cached.library_cache;
    const isValid = cache && (Date.now() - cache.lastFetch < CACHE_DURATION) && !forceRefresh;

    let movies, series, continueWatching;

    if (isValid) {
      movies = cache.movies;
      series = cache.series;
      continueWatching = cache.continue;
    } else {
      const allItems = await StremioAPI.getLibrary(authKey);

      movies = allItems.filter(i => i.type === 'movie' && !i.removed);
      series = allItems.filter(i => i.type === 'series' && !i.removed);
      continueWatching = allItems
        .filter(i => {
          if (i.removed && !i.temp) return false;

          if (state.dismissedNotifs && state.dismissedNotifs[i._id]) {
            if (i.type === 'series' && countNewEpisodes(i) > 0) return true;
            const lastWatched = i.state?.lastWatched ? new Date(i.state.lastWatched).getTime() : 0;
            if (state.dismissedNotifs[i._id] >= lastWatched) return false;
          }

          if (i.state) {
            const offset = i.state.timeOffset || i.state.timeWatched || 0;
            const duration = i.state.duration || 0;
            if (offset > 0 && (duration <= 0 || (offset / duration) < 0.95)) return true;
          }

          if (i.type === 'series' && countNewEpisodes(i) > 0) return true;

          return false;
        })
        .sort((a, b) => {
          const ta = (a.state && a.state.lastWatched) || a._mtime || 0;
          const tb = (b.state && b.state.lastWatched) || b._mtime || 0;
          return new Date(tb).getTime() - new Date(ta).getTime();
        });

      const isStartedOrWatched = (i) => isWatchedItem(i) || (i.state && (i.state.overallTimeWatched > 0 || i.state.timeWatched > 0 || i.state.timeOffset > 0));
      const totalWatched = allItems.filter(isStartedOrWatched).length;

      const newCache = { movies, series, continue: continueWatching, stats: { totalWatched }, lastFetch: Date.now() };
      await chrome.storage.local.set({ library_cache: newCache });
    }

    let filteredMovies = [...movies];
    let filteredSeries = [...series];

    if (filterType === 'watched') {
      filteredMovies = filteredMovies.filter(i => isWatchedItem(i));
      filteredSeries = filteredSeries.filter(i => isWatchedItem(i));
    } else if (filterType === 'notwatched') {
      filteredMovies = filteredMovies.filter(i => !isWatchedItem(i));
      filteredSeries = filteredSeries.filter(i => !isWatchedItem(i));
    }

    const sortFn = (a, b) => {
      if (sortType === 'name') return (a.name || '').localeCompare(b.name || '');
      if (sortType === 'namereverse') return (b.name || '').localeCompare(a.name || '');
      return new Date(b.state?.lastWatched || b._mtime || 0).getTime() - new Date(a.state?.lastWatched || a._mtime || 0).getTime();
    };

    filteredMovies.sort(sortFn);
    filteredSeries.sort(sortFn);

    return { movies: filteredMovies, series: filteredSeries, continue: continueWatching };
  },

  async invalidate() {
    await chrome.storage.local.remove(['library_cache']);
  }
};

// ==================== UI Helpers ====================
function createSkeletonCards(count = 6) {
  const fragment = document.createDocumentFragment();
  for (let i = 0; i < count; i++) {
    const card = document.createElement('div');
    card.className = 'item-card skeleton-card';
    card.innerHTML = `
      <div class="skeleton skeleton-poster"></div>
      <div class="item-info">
        <div class="skeleton" style="height:12px;margin-bottom:4px;"></div>
        <div class="skeleton" style="height:10px;width:50%;"></div>
      </div>
    `;
    fragment.appendChild(card);
  }
  return fragment;
}

function createSkeletonList(count = 3) {
  const fragment = document.createDocumentFragment();
  for (let i = 0; i < count; i++) {
    const card = document.createElement('div');
    card.className = 'continue-card';
    card.style.pointerEvents = 'none';
    card.innerHTML = `
      <div class="skeleton" style="width:50px;height:75px;border-radius:6px;flex-shrink:0;"></div>
      <div class="continue-info" style="flex:1;">
        <div class="skeleton" style="height:14px;width:70%;margin-bottom:6px;"></div>
        <div class="skeleton" style="height:10px;width:45%;margin-bottom:8px;"></div>
        <div class="skeleton" style="height:3px;width:100%;"></div>
      </div>
    `;
    fragment.appendChild(card);
  }
  return fragment;
}

function showToast(message, type = 'info', duration = 2500) {
  const existing = document.getElementById('sh-toast');
  if (existing) existing.remove();

  const toast = document.createElement('div');
  toast.id = 'sh-toast';
  toast.className = `toast toast-${type}`;

  let iconHtml = '';
  if (type === 'success') {
    iconHtml = `<svg class="toast-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg>`;
  } else if (type === 'error') {
    iconHtml = `<svg class="toast-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="15" y1="9" x2="9" y2="15"></line><line x1="9" y1="9" x2="15" y2="15"></line></svg>`;
  } else {
    iconHtml = `<svg class="toast-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line></svg>`;
  }

  toast.innerHTML = `
    <div class="toast-content">
      ${iconHtml}
      <span>${message}</span>
    </div>
  `;

  const container = document.getElementById('toast-container');
  if (container) {
    container.appendChild(toast);
  } else {
    document.getElementById('app').appendChild(toast);
  }

  requestAnimationFrame(() => {
    toast.classList.add('toast-visible');
  });

  setTimeout(() => {
    toast.classList.remove('toast-visible');
    setTimeout(() => toast.remove(), 300);
  }, duration);
}

// ==================== State ====================
const state = {
  language: 'ar',
  currentScreen: 'login',
  currentTab: 'movies',
  currentDetail: null,
  auth: null,
  library: { movies: [], series: [], continue: [] },
  addons: [],
  searchResults: [],
  isFromSearch: false,
  searchFilter: 'all',
  showLibrarySort: true,
  popupSize: 'default',
  metaAddon: 'auto',
  librarySort: 'lastwatched',
  dismissedNotifs: {},
  stremioNotifications: null,
  openMethod: 'web',
  detailMode: 'fullscreen',
  toastEnabled: true,
  toastPosition: 'bottom-right',
  siteActions: {
    google: 'save',
    letterboxd: 'save',
    imdb: 'save',
    tmdb: 'save',
    rt: 'save',
    metacritic: 'save',
    trakt: 'save'
  },
  sitesEnabled: {
    google: true,
    letterboxd: true,
    imdb: true,
    tmdb: true,
    rt: true,
    metacritic: true,
    trakt: true
  }
};

// ==================== DOM Cache ====================
const $ = (id) => document.getElementById(id);
const $$ = (sel) => document.querySelectorAll(sel);

// ==================== Init ====================
document.addEventListener('DOMContentLoaded', async () => {
  const localCache = await chrome.storage.local.get(['stremio_auth', 'libraryFilter', 'librarySort', 'autoSave', 'showLibrarySort', 'popupSize', 'detailMode', 'metaAddon', 'dismissedNotifs', 'sitesEnabled', 'openMethod', 'siteActions', 'language', 'toastEnabled', 'toastPosition']);

  if (localCache.language) state.language = localCache.language;
  applyI18N(state.language);
  if ($('setting-language')) $('setting-language').value = state.language;

  if (localCache.stremio_auth) state.auth = localCache.stremio_auth;
  if (localCache.autoSave !== undefined) state.autoSave = localCache.autoSave;
  if (localCache.showLibrarySort !== undefined) state.showLibrarySort = localCache.showLibrarySort;
  if (localCache.libraryFilter) state.libraryFilter = localCache.libraryFilter;
  if (localCache.librarySort) state.librarySort = localCache.librarySort;
  if (localCache.popupSize) state.popupSize = localCache.popupSize;
  if (localCache.detailMode) state.detailMode = localCache.detailMode;
  if (localCache.metaAddon) state.metaAddon = localCache.metaAddon;
  if (localCache.sitesEnabled) state.sitesEnabled = localCache.sitesEnabled;
  if (localCache.openMethod) state.openMethod = localCache.openMethod;
  if (localCache.siteActions) state.siteActions = localCache.siteActions;
  if (localCache.toastEnabled !== undefined) state.toastEnabled = localCache.toastEnabled;
  if (localCache.toastPosition) state.toastPosition = localCache.toastPosition;

  if ($('setting-autosave')) $('setting-autosave').checked = state.autoSave;
  if ($('setting-show-sort')) $('setting-show-sort').checked = state.showLibrarySort;
  if ($('setting-toast-enabled')) $('setting-toast-enabled').checked = state.toastEnabled;
  if ($('setting-toast-position')) $('setting-toast-position').value = state.toastPosition;
  if ($('setting-popup-size')) $('setting-popup-size').value = state.popupSize || 'default';
  if ($('setting-detail-mode')) {
    $('setting-detail-mode').value = state.detailMode || 'fullscreen';
    document.body.classList.toggle('detail-mode-card', state.detailMode === 'card');
  }
  if ($('setting-open-method')) $('setting-open-method').value = state.openMethod;


  if ($('setting-language')) {
    $('setting-language').addEventListener('change', (e) => {
      const newLang = e.target.value;
      state.language = newLang;
      chrome.storage.local.set({ language: newLang });
      applyI18N(newLang);
    });
  }
  // Sync sites toggles and actions
  const sites = ['google', 'letterboxd', 'imdb', 'tmdb', 'rt', 'metacritic', 'trakt'];
  sites.forEach(site => {
    const el = $(`site-${site}`);
    if (el) el.checked = state.sitesEnabled[site] !== false;
    const actionEl = $(`action-${site}`);
    if (actionEl) actionEl.value = state.siteActions[site] || 'save';
  });

  if (state.popupSize === 'compact') {
    document.body.classList.add('compact-mode');
  }

  state.dismissedNotifs = localCache.dismissedNotifs || {};

  updateLibrarySortUI();

  if (state.auth?.authKey) {
    showScreen('main');
    await loadLibrary();
    await checkPendingSearch();
    // Initialize tab indicator position
    setTimeout(() => {
      switchTab(state.currentTab);
    }, 50);
  } else {
    showScreen('login');
  }

  setupEventListeners();
});

// ==================== Screen Management ====================
function showScreen(name) {
  $$('.screen').forEach(s => s.classList.add('hidden'));
  $(`screen-${name}`).classList.remove('hidden');
  state.currentScreen = name;

  if (name === 'settings') {
    const emailEl = $('settings-user-email');
    if (emailEl) {
      emailEl.textContent = state.auth?.email || 'غير معروف';
    }
    // Initialize the tab position on screen open
    setTimeout(() => switchSettingsTab('general'), 10);
  }
}

// ==================== Event Listeners ====================
function setupEventListeners() {
  $('login-btn').addEventListener('click', handleLogin);
  $('password-input').addEventListener('keydown', (e) => {
    if (e.key === 'Enter') handleLogin();
  });

  $$('.tab-btn').forEach(btn => {
    btn.addEventListener('click', () => switchTab(btn.dataset.tab));
  });

  $$('.settings-tab-btn').forEach(btn => {
    btn.addEventListener('click', () => switchSettingsTab(btn.dataset.settingsTab));
  });

  const contentArea = document.querySelector('.content-area');
  if (contentArea) {
    contentArea.addEventListener('scroll', () => {
      const sortBar = $('library-sort-bar');
      if (sortBar) {
        if (contentArea.scrollTop > 10) {
          sortBar.classList.add('scrolled');
        } else {
          sortBar.classList.remove('scrolled');
        }
      }
    });
  }

  const searchToggle = $('search-toggle');
  if (searchToggle) searchToggle.addEventListener('click', toggleSearch);

  const closeSearchModal = $('close-search-modal');
  if (closeSearchModal) closeSearchModal.addEventListener('click', toggleSearch);

  const globalSearchInput = $('global-search-input');
  if (globalSearchInput) globalSearchInput.addEventListener('input', debounce(handleSearchInput, 450));

  const localFilterInput = $('local-filter-input');
  if (localFilterInput) {
    localFilterInput.addEventListener('input', (e) => {
      const term = e.target.value.toLowerCase().trim();
      const activeTab = document.querySelector('.tab-content.active');
      if (!activeTab) return;
      const cards = activeTab.querySelectorAll('.item-card');
      cards.forEach(card => {
        const titleEl = card.querySelector('.card-title');
        if (titleEl) {
          const title = titleEl.textContent.toLowerCase();
          if (title.includes(term)) {
            card.style.display = '';
          } else {
            card.style.display = 'none';
          }
        }
      });
    });
  }

  const backBtn = $('back-btn');
  if (backBtn) backBtn.addEventListener('click', goBack);

  const overlayBg = document.querySelector('.detail-overlay-bg');
  if (overlayBg) {
    overlayBg.addEventListener('click', () => {
      if (state.detailMode === 'card') {
        goBack();
      }
    });
  }

  const btnWeb = $('btn-web');
  if (btnWeb) btnWeb.addEventListener('click', () => {
    if (state.openMethod === 'app') openStremioApp();
    else openStremioWeb();
  });

  const btnApp = $('btn-app');
  if (btnApp) btnApp.addEventListener('click', () => {
    if (state.openMethod === 'app') openStremioWeb();
    else openStremioApp();
  });

  const btnAdd = $('btn-add');
  if (btnAdd) btnAdd.addEventListener('click', handleAddToLibrary);

  const btnWatched = $('btn-watched');
  if (btnWatched) btnWatched.addEventListener('click', handleMarkWatched);

  const btnRemove = $('btn-remove');
  if (btnRemove) btnRemove.addEventListener('click', handleRemove);

  const refreshBtnTop = $('refresh-btn');
  if (refreshBtnTop) refreshBtnTop.addEventListener('click', () => loadLibrary(true));
  const logoutBtn = $('logout-btn');
  if (logoutBtn) logoutBtn.addEventListener('click', handleLogout);

  const translateBtn = $('btn-translate');
  if (translateBtn) translateBtn.addEventListener('click', handleTranslateDescription);

  $('search-toggle').addEventListener('click', toggleSearch);
  $('settings-btn').addEventListener('click', () => showScreen('settings'));
  $('settings-back-btn').addEventListener('click', () => showScreen('main'));

  const refreshBtn = $('refresh-btn');
  if (refreshBtn) {
    refreshBtn.addEventListener('click', () => {
      refreshBtn.classList.add('rotating');
      loadLibrary(true).finally(() => {
        setTimeout(() => refreshBtn.classList.remove('rotating'), 500);
      });
    });
  }

  $('logout-btn').addEventListener('click', handleLogout);

  $('setting-autosave').addEventListener('change', (e) => {
    state.autoSave = e.target.checked;
    chrome.storage.local.set({ autoSave: state.autoSave });
  });

  if ($('setting-toast-enabled')) {
    $('setting-toast-enabled').addEventListener('change', (e) => {
      state.toastEnabled = e.target.checked;
      chrome.storage.local.set({ toastEnabled: state.toastEnabled });
    });
  }

  if ($('setting-toast-position')) {
    $('setting-toast-position').addEventListener('change', (e) => {
      state.toastPosition = e.target.value;
      chrome.storage.local.set({ toastPosition: state.toastPosition });
    });
  }

  $('setting-show-sort').addEventListener('change', (e) => {
    state.showLibrarySort = e.target.checked;
    chrome.storage.local.set({ showLibrarySort: state.showLibrarySort });
    updateLibrarySortUI();
  });

  const settingPopupSize = $('setting-popup-size');
  const settingDetailMode = $('setting-detail-mode');

  if (settingDetailMode) {
    settingDetailMode.addEventListener('change', async (e) => {
      state.detailMode = e.target.value;
      document.body.classList.toggle('detail-mode-card', state.detailMode === 'card');
      await chrome.storage.local.set({ detailMode: state.detailMode });
      updateBackBtnIcon();
    });
  }
  if (settingPopupSize) {
    settingPopupSize.addEventListener('change', (e) => {
      state.popupSize = e.target.value;
      chrome.storage.local.set({ popupSize: state.popupSize });
      if (state.popupSize === 'compact') {
        document.body.classList.add('compact-mode');
      } else {
        document.body.classList.remove('compact-mode');
      }
    });
  }

  const settingOpenMethod = $('setting-open-method');
  if ($('setting-open-method')) {
    $('setting-open-method').addEventListener('change', (e) => {
      state.openMethod = e.target.value;
      chrome.storage.local.set({ openMethod: state.openMethod });
      updateWatchButtonsUI();
    });
  }

  if ($('setting-meta-addon')) {
    $('setting-meta-addon').value = state.metaAddon;
    $('setting-meta-addon').addEventListener('change', (e) => {
      state.metaAddon = e.target.value;
      chrome.storage.local.set({ metaAddon: state.metaAddon });
    });
  }

  const sites = ['google', 'letterboxd', 'imdb', 'tmdb', 'rt', 'metacritic', 'trakt'];
  sites.forEach(site => {
    const el = $(`site-${site}`);
    if (el) {
      el.addEventListener('change', (e) => {
        state.sitesEnabled[site] = e.target.checked;
        chrome.storage.local.set({ sitesEnabled: state.sitesEnabled });
      });
    }
    const actionEl = $(`action-${site}`);
    if (actionEl) {
      actionEl.addEventListener('change', (e) => {
        state.siteActions[site] = e.target.value;
        chrome.storage.local.set({ siteActions: state.siteActions });
      });
    }
  });

  // Library Sort Buttons
  $$('.sort-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      $$('.sort-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      state.librarySort = btn.dataset.sort;
      chrome.storage.local.set({ librarySort: state.librarySort });
      loadLibrary(false);
    });
  });

  $$('.filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      $$('.filter-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      state.libraryFilter = btn.dataset.filter;
      chrome.storage.local.set({ libraryFilter: state.libraryFilter });
      loadLibrary(false);
    });
  });
}

function updateLibrarySortUI() {
  const bar = $('library-sort-bar');
  if (state.showLibrarySort && state.currentTab !== 'search-results') {
    bar.classList.remove('hidden');
  } else {
    bar.classList.add('hidden');
  }

  $$('.sort-btn').forEach(b => {
    if (b.dataset.sort === state.librarySort) {
      b.classList.add('active');
    } else {
      b.classList.remove('active');
    }
  });

  $$('.filter-btn').forEach(b => {
    if (b.dataset.filter === (state.libraryFilter || 'all')) {
      b.classList.add('active');
    } else {
      b.classList.remove('active');
    }
  });
}

// ==================== Login ====================
async function handleLogin() {
  const email = $('email-input').value.trim();
  const password = $('password-input').value;

  if (!email || !password) {
    showLoginError(I18N[state.language || 'ar'].msg_enter_credentials);
    return;
  }

  setLoginLoading(true);

  try {
    const authResult = await StremioAPI.login(email, password);
    if (authResult) {
      state.auth = authResult;
      await chrome.storage.local.set({ stremio_auth: authResult });
      showScreen('main');
      await loadLibrary();
    } else {
      showLoginError(I18N[state.language || 'ar'].msg_invalid_credentials);
    }
  } catch (e) {
    console.error('Login error:', e);
    showLoginError(I18N[state.language || 'ar'].msg_network_error);
  }

  setLoginLoading(false);
}

function setLoginLoading(loading) {
  const btn = $('login-btn');
  btn.querySelector('.btn-text').classList.toggle('hidden', loading);
  btn.querySelector('.btn-loader').classList.toggle('hidden', !loading);
  btn.disabled = loading;
  $('email-input').disabled = loading;
  $('password-input').disabled = loading;
}

function showLoginError(msg) {
  const el = $('login-error');
  el.textContent = msg;
  el.classList.remove('hidden');
  setTimeout(() => el.classList.add('hidden'), 4000);
}

// ==================== Logout ====================
async function handleLogout() {
  await chrome.storage.local.remove(['stremio_auth', 'library_cache']);
  state.auth = null;
  state.library = { movies: [], series: [], continue: [] };
  showScreen('login');
  showToast(I18N[state.language || 'ar'].msg_logged_out, 'info');
}

// ==================== Library Loading ====================
async function loadLibrary(forceRefresh = false) {
  if (!state.auth) return;
  $('movies-grid').innerHTML = '';
  $('movies-grid').appendChild(createSkeletonCards(6));
  $('series-grid').innerHTML = '';
  $('series-grid').appendChild(createSkeletonCards(6));
  $('continue-list').innerHTML = '';
  $('continue-list').appendChild(createSkeletonList(3));

  try {
    const libraryData = await Library.get(state.auth.authKey, forceRefresh, state.librarySort, state.libraryFilter);
    state.library.movies = libraryData.movies;
    state.library.series = libraryData.series;
    state.library.continue = libraryData.continue;

    renderMovies();
    renderSeries();
    renderContinue();

    // Render Stats
    chrome.storage.local.get(['library_cache']).then(cached => {
      const cache = cached.library_cache;
      if (cache) {
        let wCount = 0;
        if (cache.stats && cache.stats.totalWatched !== undefined) {
          wCount = cache.stats.totalWatched;
        } else {
          const allM = cache.movies || [];
          const allS = cache.series || [];
          const isStartedOrWatched = (i) => isWatchedItem(i) || (i.state && (i.state.overallTimeWatched > 0 || i.state.timeWatched > 0 || i.state.timeOffset > 0));
          wCount = allM.filter(isStartedOrWatched).length + allS.filter(isStartedOrWatched).length;
        }
        const statWatched = $('stat-watched');
        if (statWatched) statWatched.textContent = wCount;
      }
    });

    StremioAPI.getAddons(state.auth.authKey).then(addons => {
      state.addons = addons;
      const statAddons = $('stat-addons');
      if (statAddons) statAddons.textContent = addons.length;

      const metaSelect = $('setting-meta-addon');
      if (metaSelect) {
        metaSelect.innerHTML = '';

        addons.forEach(addon => {
          if (!addon.manifest || !addon.manifest.resources) return;
          let supportsMeta = false;
          if (Array.isArray(addon.manifest.resources)) {
            addon.manifest.resources.forEach(res => {
              if (typeof res === 'string' && res === 'meta') {
                if (!addon.manifest.types || addon.manifest.types.includes('movie') || addon.manifest.types.includes('series')) {
                  supportsMeta = true;
                }
              } else if (typeof res === 'object' && res.name === 'meta') {
                if (!res.types || res.types.includes('movie') || res.types.includes('series')) {
                  supportsMeta = true;
                }
              }
            });
          }
          if (supportsMeta) {
            const opt = document.createElement('option');
            const baseUrl = addon.transportUrl.replace('/manifest.json', '');
            opt.value = baseUrl;
            opt.textContent = addon.manifest.name || 'Unknown Addon';
            metaSelect.appendChild(opt);
          }
        });
        if ((!state.metaAddon || state.metaAddon === 'auto') && metaSelect.options.length > 0) {
          state.metaAddon = metaSelect.options[0].value;
          chrome.storage.local.set({ metaAddon: state.metaAddon });
        }
        metaSelect.value = state.metaAddon;
      }
    }).catch(err => console.error("Failed to load addons", err));

    const allItems = [...state.library.movies, ...state.library.series, ...state.library.continue];
    fetchNotifications(allItems).then(() => {
      // Re-filter continue watching with new episode data
      if (state.auth) {
        Library.get(state.auth.authKey, true, state.librarySort, state.libraryFilter).then(d => {
          state.library.continue = d.continue;
          renderContinue(true);
        });
      }
    });

    if (forceRefresh) showToast(I18N[state.language || 'ar'].msg_updated, 'success');
  } catch (e) {
    console.error('Library load failed:', e);
    if (e.message?.includes('NotAuthorized') || e.message?.includes('401')) {
      await handleLogout();
      showToast(I18N[state.language || 'ar'].msg_session_expired, 'error');
    } else {
      showToast(I18N[state.language || 'ar'].msg_library_error, 'error');
    }
  }
}

// ==================== Render Functions ====================
function renderSearchResults() {
  const grid = $('search-results-grid');
  grid.innerHTML = '';

  let results = state.searchResults;
  if (state.searchFilter !== 'all') {
    results = results.filter(i => i.type === state.searchFilter);
  }

  if (results.length === 0) {
    grid.innerHTML = `<div class="empty-state" style="grid-column:1/-1;">
      <div class="empty-state-icon">🔍</div><div>لم يتم العثور على نتائج</div></div>`;
    return;
  }

  results.forEach((item, index) => {
    const card = createItemCard(item);
    card.style.animationDelay = `${index * 30}ms`;
    grid.appendChild(card);
  });
}

function renderMovies() {
  const grid = $('movies-grid');
  grid.innerHTML = '';

  if (!state.library.movies.length) {
    const t = I18N[state.language || 'ar'];
    grid.innerHTML = `<div class="empty-state" style="grid-column:1/-1;">
      <div class="empty-state-icon">🎬</div><div>${t.empty_movies || 'مكتبة الأفلام فارغة'}</div></div>`;
    return;
  }

  state.library.movies.forEach((item, index) => {
    const card = createItemCard(item);
    card.style.animationDelay = `${index * 30}ms`;
    grid.appendChild(card);
  });
}

function renderSeries() {
  const grid = $('series-grid');
  grid.innerHTML = '';

  if (!state.library.series.length) {
    const t = I18N[state.language || 'ar'];
    grid.innerHTML = `<div class="empty-state" style="grid-column:1/-1;">
      <div class="empty-state-icon">📺</div><div>${t.empty_series || 'لا توجد مسلسلات في مكتبتك'}</div></div>`;
    return;
  }

  state.library.series.forEach((item, index) => {
    const card = createItemCard(item);
    card.style.animationDelay = `${index * 30}ms`;
    grid.appendChild(card);
  });
}

function renderContinue(silent = false) {
  const list = $('continue-list');
  list.innerHTML = '';
  if (!state.library.continue || !state.library.continue.length) {
    const t = I18N[state.language || 'ar'];
    list.innerHTML = `<div class="empty-state">
      <div class="empty-state-icon">▶️</div><div>${t.empty_continue || 'لا يوجد شيء قيد المشاهدة'}</div></div>`;
    return;
  }
  state.library.continue.forEach((item, index) => {
    const card = createItemCard(item, true);
    if (silent) {
      card.style.animation = 'none';
      card.style.opacity = '1';
    } else {
      card.style.animationDelay = `${index * 40}ms`;
    }
    list.appendChild(card);
  });
}

// ==================== Card Creators ====================
function createItemCard(item, isCW = false) {
  let progress = 0;
  if (item.state) {
    if (item.state.timeOffset > 0 && item.state.duration > 0) {
      progress = (item.state.timeOffset / item.state.duration) * 100;
    } else if (item.state.timeWatched > 0 && item.state.duration > 0) {
      progress = (item.state.timeWatched / item.state.duration) * 100;
    } else if (item.state.timeOffset > 0 || item.state.timeWatched > 0 || item.state.overallTimeWatched > 0) {
      progress = 35;
    }
  }
  progress = Math.min(Math.max(progress, 0), 100);

  let epBadge = '';
  if (item.type === 'series' && item.state?.video_id) {
    const parts = item.state.video_id.split(':');
    if (parts.length >= 3) {
      epBadge = `S${parts[1]} E${parts[2]}`;
    }
  }

  const card = document.createElement('div');
  card.className = 'item-card';
  if (isCW) card.classList.add('continue-card');
  card.dataset.id = item._id || '';

  const isWatched = isWatchedItem(item);
  let badgeHTML = isWatched ? `<div class="watched-badge"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg></div>` : '';

  const hasNewEpsCount = countNewEpisodes(item);
  let dismissHTML = '';
  if (hasNewEpsCount > 0) {
    badgeHTML = `<div class="new-ep-badge">+${hasNewEpsCount}</div>`;
    dismissHTML = `<div class="dismiss-btn"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg></div>`;
  } else if (isCW && progress > 0) {
    dismissHTML = `<div class="dismiss-btn"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg></div>`;
  }

  card.innerHTML = `
    <img src="${item.poster || ''}" alt="${escapeHTML(item.name)}" loading="lazy">
    ${badgeHTML}
    ${dismissHTML}
    ${epBadge ? `<div class="continue-ep-badge">${epBadge}</div>` : ''}
    <div class="card-label pos-top">
      <div class="card-title">${escapeHTML(item.name)}</div>
      ${item.imdbRating ? `<div class="card-rating">★ ${item.imdbRating}</div>` : ''}
    </div>
    ${progress > 0 ? `
    <div class="poster-progress">
      <div class="poster-progress-fill" style="width:${progress.toFixed(1)}%"></div>
    </div>` : ''}
  `;
  card.addEventListener('click', (e) => {
    if (e.target.closest('.dismiss-btn')) {
      e.stopPropagation();
      dismissNotification(item, card);
      return;
    }
    openDetail(item);
  });
  return card;
}

// ==================== Detail View ====================
async function openDetail(libraryItem) {
  const screenDetail = $('screen-detail');
  screenDetail.classList.remove('card-fade-out'); // clear any exit animation

  if (state.detailMode === 'card') {
    screenDetail.classList.remove('hidden');
    screenDetail.classList.add('show-modal');
  } else {
    showScreen('detail');
    $('screen-detail').classList.remove('show-modal');
  }

  updateBackBtnIcon();

  state.isFromSearch = state.currentTab === 'search-results';

  const searchModal = $('search-modal');
  if (searchModal && !searchModal.classList.contains('hidden')) {
    searchModal.classList.add('hidden');
  }

  const imdbId = libraryItem._id || libraryItem.imdb_id || libraryItem.id;
  const type = libraryItem.type;

  renderDetailBasic(libraryItem);

  const isInLibrary = state.library.movies.some(m => m._id === imdbId) ||
    state.library.series.some(s => s._id === imdbId);

  const btnAdd = $('btn-add');
  const btnRemove = $('btn-remove');
  const btnWatched = $('btn-watched');

  btnAdd.disabled = false;
  btnRemove.disabled = false;
  btnWatched.disabled = false;

  btnAdd.classList.toggle('hidden', isInLibrary);
  btnWatched.classList.toggle('hidden', !isInLibrary);
  btnRemove.classList.toggle('hidden', !isInLibrary);

  if (isWatchedItem(libraryItem)) {
    $('btn-watched').innerHTML = `
      <div class="icon-circle" style="background: rgba(16, 185, 129, 0.2); color: #10b981;">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
      </div>
      <span style="color: #10b981;" data-i18n="marked_watched">${I18N[state.language || 'ar'].marked_watched || 'شُوهد'}</span>
    `;
  } else {
    $('btn-watched').innerHTML = `
      <div class="icon-circle">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      </div>
      <span data-i18n="mark_watched">${I18N[state.language || 'ar'].mark_watched || 'تحديد كمشاهد'}</span>
    `;
  }

  try {
    const meta = await SearchEngine.getMeta(type, imdbId);
    state.currentDetail = { meta: { ...meta, _id: imdbId, id: imdbId, imdb_id: imdbId, type: type || 'movie' }, libraryItem: { ...libraryItem, _id: imdbId } };
    renderDetail(meta, libraryItem);
  } catch (e) {
    console.error('Cinemeta fetch failed:', e);
    state.currentDetail = {
      meta: {
        name: libraryItem.name, poster: libraryItem.poster,
        year: libraryItem.year || libraryItem.releaseInfo,
        description: '', cast: [], genres: [],
        _id: imdbId, id: imdbId, imdb_id: imdbId, type: type || 'movie'
      },
      libraryItem: { ...libraryItem, _id: imdbId }
    };
  }
}

function renderDetailBasic(item) {
  const bg = document.querySelector('.detail-bg');
  bg.style.backgroundImage = `url('${item.poster || ''}')`;
  $('detail-poster').src = item.poster || '';
  $('detail-title').textContent = item.name || '';
  $('detail-year').textContent = item.year || item.releaseInfo || '';
  $('detail-runtime').textContent = '';
  $('detail-rating').textContent = item.imdbRating ? `⭐ ${item.imdbRating}` : '';
  $('detail-genres').innerHTML = '';
  const descEl = $('detail-description');
  descEl.textContent = '';
  delete descEl.dataset.original;

  const btnTranslate = $('btn-translate');
  if (btnTranslate) {
    btnTranslate.disabled = false;
    const t = I18N[state.language || 'ar'];
    btnTranslate.innerHTML = `
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/>
      </svg>
      <span data-i18n="translate_btn">${t.translate_btn || 'ترجمة'}</span>
    `;
  }

  $('detail-cast').innerHTML = '';

  const progressEl = $('detail-watch-progress');
  if (progressEl) progressEl.innerHTML = '';

  updateWatchButtonsUI();
}

function updateWatchButtonsUI() {
  const isApp = state.openMethod === 'app';
  const lang = state.language || 'ar';
  const t = I18N[lang];

  const mainBtnText = $('btn-web').querySelector('span');
  if (mainBtnText) {
    if (state.currentDetail && state.currentDetail.activeVideo) {
      const v = state.currentDetail.activeVideo;
      mainBtnText.textContent = t.play_episode ? t.play_episode.replace('{s}', v.season).replace('{e}', v.episode) : `شاهد S${v.season}E${v.episode} الآن`;
    } else {
      mainBtnText.textContent = t.watch_now || "مشاهدة الآن";
    }
  }

  const secondaryBtn = $('btn-app');
  if (secondaryBtn) {
    const span = secondaryBtn.querySelector('span');
    const iconCircle = secondaryBtn.querySelector('.icon-circle');

    if (isApp) {
      secondaryBtn.title = I18N[lang].web_btn || "المتصفح";
      if (span) span.textContent = I18N[lang].web_btn || "المتصفح";
      if (iconCircle) iconCircle.innerHTML = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>`;
    } else {
      secondaryBtn.title = I18N[lang].app_btn || "التطبيق";
      if (span) span.textContent = I18N[lang].app_btn || "التطبيق";
      if (iconCircle) iconCircle.innerHTML = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>`;
    }
  }
}

function renderDetail(meta, libraryItem) {
  const bg = document.querySelector('.detail-bg');
  if (meta.background || meta.poster) {
    bg.style.backgroundImage = `url('${meta.background || meta.poster}')`;
  }
  $('detail-poster').src = meta.poster || libraryItem.poster || '';
  $('detail-title').textContent = meta.name || libraryItem.name || '';
  $('detail-year').textContent = meta.year || libraryItem.year || '';
  $('detail-runtime').textContent = meta.runtime || '';

  $('detail-rating').textContent = meta.imdbRating ? `⭐ ${meta.imdbRating}` : '';

  // Smart Progress Tracking & Episodes Browser
  const progressEl = $('detail-watch-progress');
  const episodesSection = $('episodes-browser-section');
  const isSeries = libraryItem.type === 'series';
  const t = I18N[state.language || 'ar'];

  if (isSeries && meta.videos && meta.videos.length > 0) {
    const sortedVideos = meta.videos.sort((a, b) => (a.season === 0 ? Infinity : a.season) - (b.season === 0 ? Infinity : b.season) || a.episode - b.episode);
    const s = libraryItem.state || {};
    const curIdx = sortedVideos.findIndex(v => v.id === s.video_id);

    let activeVideo = null;
    let activeStatus = '';

    if (curIdx !== -1) {
      const isFinished = s.timeOffset > 0 && s.duration > 0 && (s.timeOffset / s.duration) >= 0.95;
      if (!isFinished && s.timeOffset > 0) {
        activeStatus = 'resume';
        activeVideo = sortedVideos[curIdx];
      } else if (curIdx + 1 < sortedVideos.length) {
        activeStatus = 'next';
        activeVideo = sortedVideos[curIdx + 1];
      }
    }
    if (!activeVideo) {
      activeStatus = 'start';
      activeVideo = sortedVideos[0];
    }

    state.currentDetail.activeVideo = activeVideo;
    state.currentDetail.activeStatus = activeStatus;

    if (progressEl) {
      let watchInfo = '';
      if (activeStatus === 'resume') {
        const text = t.resume_episode ? t.resume_episode.replace('{s}', activeVideo.season).replace('{e}', activeVideo.episode) : `استكمال S${activeVideo.season}E${activeVideo.episode}`;
        watchInfo += `<span class="meta-chip highlight-chip" style="border-color:var(--accent);color:var(--accent-light);">${text}</span>`;
      } else if (activeStatus === 'next') {
        const text = t.next_episode ? t.next_episode.replace('{s}', activeVideo.season).replace('{e}', activeVideo.episode) : `الحلقة التالية: S${activeVideo.season}E${activeVideo.episode}`;
        watchInfo += `<span class="meta-chip highlight-chip" style="border-color:var(--success);color:var(--success);">${text}</span>`;
      } else if (s.video_id) {
        const parts = s.video_id.split(':');
        if (parts.length >= 3) {
          const seasonNum = parseInt(parts[1], 10);
          let seasonText = `الموسم ${parts[1]}`;
          if (seasonNum === 0) {
            seasonText = (state.language === 'ar' || !state.language) ? 'حلقات خاصة' : 'Specials';
          } else if (t.season_n) {
            seasonText = t.season_n.replace('{n}', parts[1]);
          }
          watchInfo += `<span class="meta-chip highlight-chip" style="border-color:var(--accent);color:var(--accent-light);">${seasonText} · الحلقة ${parts[2]}</span>`;
        }
      }

      const hasNewEpsCount = countNewEpisodes(libraryItem);
      if (hasNewEpsCount > 0) {
        const text = t.new_episodes_count ? t.new_episodes_count.replace('{count}', hasNewEpsCount) : `${hasNewEpsCount} حلقات جديدة`;
        watchInfo += `<span class="new-episodes-count-badge">
          ${text}
          <button class="mark-read-btn" title="${t.mark_read_title || 'تحديد كمقروء'}">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
          </button>
        </span>`;
      }

      const totalMin = Math.floor((s.duration || 0) / 60000);
      const watchedMin = Math.floor((s.timeOffset || s.timeWatched || 0) / 60000);

      if (totalMin > 0 && watchedMin > 0) {
        const remainingMin = totalMin - watchedMin;
        if (remainingMin > 0) {
          watchInfo += `<span class="meta-chip">متبقي ${remainingMin} دقيقة</span>`;
        } else {
          watchInfo += `<span class="meta-chip">مكتمل</span>`;
        }
      }
      progressEl.innerHTML = watchInfo;

      const markReadBtn = progressEl.querySelector('.mark-read-btn');
      if (markReadBtn) {
        markReadBtn.addEventListener('click', (e) => {
          e.stopPropagation();
          dismissNotification(libraryItem);
          openDetail(libraryItem); // refresh
        });
      }
    }

    // Episodes Browser
    if (episodesSection) {
      episodesSection.classList.remove('hidden');
      const seasons = [...new Set(sortedVideos.map(v => v.season))];
      const seasonSelector = $('season-selector');
      seasonSelector.innerHTML = seasons.map(s => {
        let sn;
        if (s === 0) {
          sn = (state.language === 'ar' || !state.language) ? 'حلقات خاصة' : 'Specials';
        } else {
          sn = t.season_n ? t.season_n.replace('{n}', s) : `الموسم ${s}`;
        }
        return `<option value="${s}" ${s === activeVideo.season ? 'selected' : ''}>${sn}</option>`;
      }).join('');

      const allVidIds = sortedVideos.map(vid => vid.id);
      let watchedSet = new Set();
      if (s.watched) {
        watchedSet = parseWatchedBitfield(s.watched, allVidIds);
      } else if (s.timesWatched > 0) {
        allVidIds.forEach(id => watchedSet.add(id));
      }

      const renderEpisodes = (season) => {
        const eps = sortedVideos.filter(v => v.season == season);
        const now = new Date();
        const list = $('episodes-list');
        list.innerHTML = eps.map(v => {
          const isEpWatched = (s.video_id === v.id && s.timeOffset > 0 && s.duration > 0 && (s.timeOffset / s.duration) >= 0.95) || watchedSet.has(v.id);
          const isUnreleased = v.released && new Date(v.released) > now;
          const thumbHTML = isUnreleased
            ? `<div style="width:100%;height:100%;background:linear-gradient(135deg, rgba(30,30,36,0.8), rgba(20,20,26,0.8));display:flex;flex-direction:column;justify-content:center;align-items:center;backdrop-filter:blur(4px);">
                 <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,0.4)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-bottom:4px;"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
                 <span style="font-size:10px;font-weight:600;color:rgba(255,255,255,0.4);letter-spacing:0.5px;">${t.coming_soon || 'قريباً'}</span>
               </div>`
            : `<img src="${v.thumbnail || meta.background || meta.poster}" loading="lazy">`;

          return `
          <div class="episode-card ${v.id === activeVideo.id ? 'active' : ''} ${isUnreleased ? 'unreleased' : ''}" data-id="${v.id}">
            <div class="episode-thumb">
              ${thumbHTML}
              <div class="episode-badge">S${v.season} E${v.episode}</div>
              ${!isUnreleased ? `
              <button class="ep-mark-watched-btn ${isEpWatched ? 'watched' : ''}" title="${isEpWatched ? (t.unmark_read_title || 'إلغاء المشاهدة') : (t.mark_read_title || 'تحديد كمقروء')}" data-ep-id="${v.id}">
                <svg class="icon-check" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
                <svg class="icon-cross" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
              </button>
              ` : ''}
            </div>
            <div class="episode-info">
              <div class="episode-title" title="${escapeHTML(v.name || `Episode ${v.episode}`)}" ${isUnreleased ? 'style="color:rgba(255,255,255,0.5);"' : ''}>${escapeHTML(v.name || `Episode ${v.episode}`)}</div>
              <div class="episode-date" ${isUnreleased ? 'style="color:var(--accent-light);font-weight:500;"' : ''}>${v.released ? new Date(v.released).toLocaleDateString() : ''}</div>
            </div>
          </div>
          `;
        }).join('');
        list.querySelectorAll('.episode-card').forEach(card => {
          card.addEventListener('click', (e) => {
            if (e.target.closest('.ep-mark-watched-btn')) {
              e.stopPropagation();
              const vidId = e.target.closest('.ep-mark-watched-btn').dataset.epId;
              const btn = e.target.closest('.ep-mark-watched-btn');
              const isCurrentlyWatched = btn.classList.contains('watched');
              btn.style.opacity = '0.5';
              markEpisodeWatched(libraryItem, vidId, isCurrentlyWatched);
              return;
            }
            const vid = sortedVideos.find(v => v.id === card.dataset.id);
            if (vid) {
              state.currentDetail.activeVideo = vid;
              updateWatchButtonsUI();
              list.querySelectorAll('.episode-card').forEach(c => c.classList.remove('active'));
              card.classList.add('active');
            }
          });
        });
      };

      renderEpisodes(activeVideo.season);
      seasonSelector.onchange = (e) => renderEpisodes(parseInt(e.target.value));

      const markSeasonBtn = $('mark-season-watched-btn');
      if (markSeasonBtn) {
        // Find if all episodes in current season are watched
        const checkSeasonWatchedStatus = () => {
          const currentSeason = parseInt(seasonSelector.value);
          const seasonVids = sortedVideos.filter(v => v.season === currentSeason);
          const allWatched = seasonVids.length > 0 && seasonVids.every(v => {
            return (s.video_id === v.id && s.timeOffset > 0 && s.duration > 0 && (s.timeOffset / s.duration) >= 0.95) || watchedSet.has(v.id);
          });
          if (allWatched) {
            markSeasonBtn.classList.add('watched');
            $('mark-season-text').textContent = t.unmark_season_watched || 'إلغاء مشاهدة الموسم';
            markSeasonBtn.title = t.unmark_season_watched || 'إلغاء مشاهدة الموسم';
          } else {
            markSeasonBtn.classList.remove('watched');
            $('mark-season-text').textContent = t.mark_season_watched || 'مشاهدة الموسم';
            markSeasonBtn.title = t.mark_season_watched || 'مشاهدة الموسم';
          }
        };
        checkSeasonWatchedStatus();
        seasonSelector.addEventListener('change', checkSeasonWatchedStatus);

        markSeasonBtn.onclick = (e) => {
          e.stopPropagation();
          const currentSeason = parseInt(seasonSelector.value);
          const isCurrentlyWatched = markSeasonBtn.classList.contains('watched');
          markSeasonBtn.style.opacity = '0.5';
          markSeasonWatched(libraryItem, currentSeason, isCurrentlyWatched);
        };
      }
    }
  } else {
    state.currentDetail.activeVideo = null;
    state.currentDetail.activeStatus = '';
    if (progressEl) progressEl.innerHTML = '';
    if (episodesSection) episodesSection.classList.add('hidden');
  }

  $('detail-genres').innerHTML = (meta.genres || []).map(g =>
    `<span class="genre-tag">${escapeHTML(g)}</span>`
  ).join('');

  $('detail-description').textContent = meta.description || '';

  const castEl = $('detail-cast');
  const castList = meta.cast || [];
  if (castList.length === 0) {
    castEl.innerHTML = '';
  } else if (typeof castList[0] === 'string') {
    castEl.innerHTML = castList.slice(0, 8).map((name, i) => {
      const initials = name.split(' ').map(n => n[0]).join('').substring(0, 2).toUpperCase();
      return `
      <div class="cast-item">
        <div class="actor-img-placeholder" id="actor-img-${i}">
          ${initials}
        </div>
        <span>${escapeHTML(name)}</span>
      </div>
      `;
    }).join('');

    // Fetch actor images asynchronously from TVMaze API (Free, no key needed)
    castList.slice(0, 8).forEach(async (name, i) => {
      try {
        const res = await fetch(`https://api.tvmaze.com/search/people?q=${encodeURIComponent(name)}`);
        if (!res.ok) return;
        const data = await res.json();
        const imgUrl = data?.[0]?.person?.image?.medium;
        if (imgUrl) {
          const placeholder = $(`actor-img-${i}`);
          if (placeholder) {
            placeholder.innerHTML = `<img src="${imgUrl}" alt="${escapeHTML(name)}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;border:none;">`;
            placeholder.style.background = 'transparent';
          }
        }
      } catch (e) { /* ignore */ }
    });
  } else {
    castEl.innerHTML = castList.slice(0, 8).map(actor => `
      <div class="cast-item">
        <img src="${actor.photo || ''}" alt="${escapeHTML(actor.name || '')}">
        <span>${escapeHTML(actor.name || '')}</span>
      </div>
    `).join('');
  }
}

// ==================== Translate API ====================
async function handleTranslateDescription() {
  const descEl = $('detail-description');
  const btn = $('btn-translate');
  if (!descEl.textContent || btn.disabled) return;

  const t = I18N[state.language || 'ar'];

  // Toggle back to original if already translated
  if (descEl.dataset.original) {
    descEl.textContent = descEl.dataset.original;
    delete descEl.dataset.original;
    btn.innerHTML = `
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/>
      </svg>
      <span data-i18n="translate_btn">${t.translate_btn || 'ترجمة'}</span>
    `;
    return;
  }

  const originalText = descEl.textContent;
  btn.disabled = true;
  btn.innerHTML = `<span style="display:inline-block;animation:spin 1s linear infinite;">↻</span> ${t.translate_loading || 'جاري...'}`;

  try {
    const targetLang = state.language === 'ar' ? 'ar' : 'en';
    const res = await fetch(`https://api.mymemory.translated.net/get?q=${encodeURIComponent(originalText)}&langpair=autodetect|${targetLang}`);
    const data = await res.json();
    if (data && data.responseData && data.responseData.translatedText) {
      descEl.dataset.original = originalText;
      descEl.textContent = data.responseData.translatedText;
      btn.innerHTML = `✓ ${t.translate_done || 'تمت'}`;
      setTimeout(() => {
        btn.disabled = false;
        btn.innerHTML = `
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 2.13-9.36L1 10"/>
          </svg>
          <span data-i18n="translate_revert">${t.translate_revert || 'الأصلي'}</span>
        `;
      }, 3000);
    } else {
      throw new Error('Translation failed');
    }
  } catch (e) {
    console.error(e);
    btn.innerHTML = `✗ ${t.translate_error || 'خطأ'}`;
    setTimeout(() => {
      btn.disabled = false;
      btn.innerHTML = `
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/>
        </svg>
        <span data-i18n="translate_btn">${t.translate_btn || 'ترجمة'}</span>
      `;
    }, 3000);
  }
}

// ==================== Tab Navigation ====================
function switchTab(tabId) {
  state.currentTab = tabId;
  $$('.tab-btn').forEach(b => b.classList.remove('active'));
  $$('.tab-content').forEach(c => c.classList.add('hidden'));
  const btn = document.querySelector(`[data-tab="${tabId}"]`);
  if (btn) {
    btn.classList.add('active');
    // Animate the tab indicator
    const indicator = document.querySelector('.tab-indicator');
    if (indicator) {
      indicator.style.width = `${btn.offsetWidth}px`;

      // Calculate position. Handle RTL correctly if offsetLeft behaves differently
      // In RTL, offsetLeft is still relative to offsetParent's left edge.
      indicator.style.transform = `translateX(${btn.offsetLeft}px)`;
    }
  }
  const contentId = tabId === 'search-results' ? 'tab-search-results-content' : `tab-${tabId}`;
  const content = $(contentId);
  if (content) content.classList.remove('hidden');
  updateLibrarySortUI();
}

function switchSettingsTab(tabId) {
  $$('.settings-tab-btn').forEach(b => b.classList.remove('active'));
  $$('.settings-tab-content').forEach(c => c.classList.add('hidden'));
  
  const btn = document.querySelector(`[data-settings-tab="${tabId}"]`);
  if (btn) {
    btn.classList.add('active');
    const indicator = document.querySelector('.settings-tab-indicator');
    if (indicator) {
      indicator.style.width = `${btn.offsetWidth}px`;
      indicator.style.transform = `translateX(${btn.offsetLeft}px)`;
    }
  }
  
  const content = $(`settings-tab-${tabId}`);
  if (content) content.classList.remove('hidden');
}

// ==================== Search ====================
function toggleSearch() {
  const modal = $('search-modal');
  const isHidden = modal.classList.contains('hidden');
  if (isHidden) {
    modal.classList.remove('hidden');
    $('global-search-input').focus();
  } else {
    modal.classList.add('hidden');
    clearSearch();
  }
}

function clearSearch() {
  $('global-search-input').value = '';
  $('search-grid-movies').innerHTML = '';
  $('search-grid-series').innerHTML = '';
  $('search-results-movies').classList.add('hidden');
  $('search-results-series').classList.add('hidden');
  $('search-empty-state').classList.add('hidden');
  state.searchResults = [];
}

function handleSearchInput(e) {
  const query = e.target.value.trim();
  if (!query || query.length < 2) {
    if (!query) clearSearch();
    return;
  }
  performSearch(query);
}

async function performSearch(query) {
  $('search-grid-movies').innerHTML = '';
  $('search-grid-series').innerHTML = '';
  $('search-grid-movies').appendChild(createSkeletonCards(3));
  $('search-grid-series').appendChild(createSkeletonCards(3));
  $('search-results-movies').classList.remove('hidden');
  $('search-results-series').classList.remove('hidden');
  $('search-empty-state').classList.add('hidden');

  try {
    const results = await SearchEngine.search(query);
    state.searchResults = results;
    renderSearchResults(results);
  } catch (e) {
    console.error('Search failed:', e);
    $('search-results-movies').classList.add('hidden');
    $('search-results-series').classList.add('hidden');
    $('search-empty-state').classList.remove('hidden');
  }
}

function renderSearchResults(results) {
  const moviesGrid = $('search-grid-movies');
  const seriesGrid = $('search-grid-series');
  const moviesSection = $('search-results-movies');
  const seriesSection = $('search-results-series');
  const emptyState = $('search-empty-state');

  moviesGrid.innerHTML = '';
  seriesGrid.innerHTML = '';

  const movies = results.filter(r => r.type === 'movie');
  const series = results.filter(r => r.type === 'series');

  if (!movies.length && !series.length) {
    moviesSection.classList.add('hidden');
    seriesSection.classList.add('hidden');
    emptyState.classList.remove('hidden');
    return;
  }

  emptyState.classList.add('hidden');

  if (movies.length) {
    moviesSection.classList.remove('hidden');
    movies.forEach(item => moviesGrid.appendChild(createItemCard({
      _id: item.imdb_id || item.id, name: item.name, poster: item.poster,
      year: item.releaseInfo || item.year, type: item.type, imdbRating: item.imdbRating
    })));
  } else {
    moviesSection.classList.add('hidden');
  }

  if (series.length) {
    seriesSection.classList.remove('hidden');
    series.forEach(item => seriesGrid.appendChild(createItemCard({
      _id: item.imdb_id || item.id, name: item.name, poster: item.poster,
      year: item.releaseInfo || item.year, type: item.type, imdbRating: item.imdbRating
    })));
  } else {
    seriesSection.classList.add('hidden');
  }
}

// ==================== Detail Actions ====================
function openStremioWeb() {
  if (!state.currentDetail) return;
  const { libraryItem, activeVideo } = state.currentDetail;
  const imdbId = libraryItem._id || libraryItem.imdb_id;
  const videoId = activeVideo ? activeVideo.id : null;
  chrome.runtime.sendMessage({ type: 'OPEN_STREMIO_WEB', imdbId, mediaType: libraryItem.type, videoId });
}

function openStremioApp() {
  if (!state.currentDetail) return;
  const { libraryItem, activeVideo } = state.currentDetail;
  const imdbId = libraryItem._id || libraryItem.imdb_id;
  const videoId = activeVideo ? activeVideo.id : null;
  chrome.runtime.sendMessage({ type: 'OPEN_STREMIO_APP', imdbId, mediaType: libraryItem.type, videoId });
}

async function handleAddToLibrary() {
  if (!state.currentDetail || !state.auth) return;
  const { meta } = state.currentDetail;
  const btn = $('btn-add');
  btn.disabled = true;
  try {
    const success = await StremioAPI.addToLibrary(state.auth.authKey, meta);
    if (success) {
      showToast(I18N[state.language || 'ar'].msg_added_library, 'success');
      btn.classList.add('hidden');
      $('btn-watched').classList.remove('hidden');
      $('btn-remove').classList.remove('hidden');
      await Library.invalidate();
    } else {
      showToast(I18N[state.language || 'ar'].msg_add_failed, 'error');
      btn.disabled = false;
    }
  } catch (e) {
    console.error('Add to library failed:', e);
    showToast(`${I18N[state.language || 'ar'].msg_error}: ${e.message}`, 'error');
    btn.disabled = false;
  }
}

function parseWatchedBitfield(serialized, videoIds) {
  if (!serialized || typeof serialized !== 'string') return new Set();
  const parts = serialized.split(':');
  if (parts.length < 3) return new Set();

  const base64Data = parts.pop();
  const lastLength = parseInt(parts.pop(), 10);
  const lastVideoId = parts.join(':');
  const watched = new Set();

  try {
    const binaryStr = atob(base64Data);
    const bytes = new Uint8Array(binaryStr.length);
    for (let b = 0; b < binaryStr.length; b++) bytes[b] = binaryStr.charCodeAt(b);
    const inflated = window.pako ? window.pako.inflate(bytes) : pako.inflate(bytes);

    const lastVideoIdx = videoIds.indexOf(lastVideoId);
    const offset = lastVideoIdx === -1 ? 0 : (lastLength - 1) - lastVideoIdx;

    for (let i = 0; i < inflated.length * 8; i++) {
      const byteIdx = Math.floor(i / 8);
      const bitIdx = i % 8;
      if (inflated[byteIdx] & (1 << bitIdx)) {
        const vidIdx = i - offset;
        if (vidIdx >= 0 && vidIdx < videoIds.length) {
          watched.add(videoIds[vidIdx]);
        }
      }
    }
  } catch (e) {
    console.warn('parseWatchedBitfield err:', e);
  }
  return watched;
}

function encodeWatchedBitfield(watchedVideoIds, seriesId, videoIds) {
  if (!watchedVideoIds || watchedVideoIds.size === 0) return null;
  const numBytes = Math.ceil(videoIds.length / 8);
  const bytes = new Uint8Array(numBytes);
  for (let i = 0; i < videoIds.length; i++) {
    if (watchedVideoIds.has(videoIds[i])) {
      bytes[Math.floor(i / 8)] |= (1 << (i % 8));
    }
  }
  let deflated;
  try { 
    deflated = window.pako ? window.pako.deflate(bytes) : pako.deflate(bytes); 
  } catch (e) { 
    console.error('Bitfield compression failed:', e);
    throw new Error('Compression failed'); 
  }
  let binaryStr = '';
  for (let i = 0; i < deflated.length; i++) {
    binaryStr += String.fromCharCode(deflated[i]);
  }
  const base64Data = btoa(binaryStr);
  const lastVideoId = videoIds[videoIds.length - 1] || seriesId;
  const length = videoIds.length;
  return `${lastVideoId}:${length}:${base64Data}`;
}

async function saveWatchedState(item, allVideos, watchedSet, targetVideoId, tOffset, dur, isRemoved = false) {
  let watchedString = null;
  try {
    watchedString = encodeWatchedBitfield(watchedSet, item._id || item.imdb_id, allVideos);
  } catch (e) {
    showToast(I18N[state.language || 'ar'].msg_error || 'خطأ', 'error');
    console.error("Aborted saveWatchedState to prevent data loss.");
    return;
  }
  let tWatched = item.state ? item.state.timesWatched || 0 : 0;
  if (watchedSet.size === 0) tWatched = 0;
  else if (watchedSet.size === allVideos.length) tWatched = 1;

  const prevTimeWatched = item.state ? item.state.timeWatched || 0 : 0;
  const prevOverall = item.state ? item.state.overallTimeWatched || 0 : 0;

  const payloadItem = {
    ...item,
    _mtime: new Date().toISOString(),
    state: {
      ...(item.state || {}),
      video_id: targetVideoId,
      timeOffset: tOffset,
      timeWatched: Math.max(prevTimeWatched, 1),
      overallTimeWatched: Math.max(prevOverall, 1),
      duration: dur,
      watched: watchedString || null,
      timesWatched: tWatched,
      lastWatched: new Date().toISOString()
    }
  };

  if (state.auth && state.auth.authKey) {
    try {
      const resp = await fetch(`https://api.strem.io/api/datastorePut`, {
        method: 'POST',
        headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
        body: JSON.stringify({
          authKey: state.auth.authKey,
          collection: 'libraryItem',
          changes: [payloadItem]
        })
      });
      const data = await resp.json();
      if (resp.ok && !data.error) {
        const msg = isRemoved 
          ? (I18N[state.language || 'ar'].msg_removed_watched || 'تم إزالة المشاهدة')
          : (I18N[state.language || 'ar'].msg_marked_watched || 'تم التحديث بنجاح');
        showToast(msg, 'success');
        state.currentDetail.libraryItem = payloadItem;
        openDetail(payloadItem);
        Library.invalidate();
      } else {
        showToast(I18N[state.language || 'ar'].msg_error || 'خطأ', 'error');
      }
    } catch (e) {
      showToast(I18N[state.language || 'ar'].msg_error || 'خطأ', 'error');
    }
  }
}

async function markEpisodeWatched(item, videoId, remove = false) {
  const meta = state.currentDetail && state.currentDetail.meta ? state.currentDetail.meta : null;
  if (!meta || !meta.videos) return;
  const sorted = meta.videos.sort((a, b) => (a.season === 0 ? Infinity : a.season) - (b.season === 0 ? Infinity : b.season) || a.episode - b.episode);
  const allVideos = sorted.map(v => v.id);

  let targetVideoId = videoId;
  let tOffset = 1;
  let dur = 0;

  const curIdx = sorted.findIndex(v => v.id === videoId);
  if (remove) {
    targetVideoId = videoId;
    tOffset = 0;
    dur = 1;
  } else {
    if (curIdx !== -1 && curIdx + 1 < sorted.length) {
      targetVideoId = sorted[curIdx + 1].id;
      tOffset = 0;
      dur = 0;
    } else {
      targetVideoId = videoId;
      tOffset = 1;
      dur = 1;
    }
  }

  let watchedSet = new Set();
  if (item.state && typeof item.state.watched === 'string') {
    watchedSet = parseWatchedBitfield(item.state.watched, allVideos);
  } else if (item.state && (item.state.timesWatched || 0) > 0) {
    allVideos.forEach(v => watchedSet.add(v));
  }

  if (remove) watchedSet.delete(videoId);
  else watchedSet.add(videoId);

  await saveWatchedState(item, allVideos, watchedSet, targetVideoId, tOffset, dur);
}

async function markSeasonWatched(item, season, remove = false) {
  const meta = state.currentDetail && state.currentDetail.meta ? state.currentDetail.meta : null;
  if (!meta || !meta.videos) return;
  const sorted = meta.videos.sort((a, b) => (a.season === 0 ? Infinity : a.season) - (b.season === 0 ? Infinity : b.season) || a.episode - b.episode);
  const allVideos = sorted.map(v => v.id);

  const seasonVideos = sorted.filter(v => v.season === season);
  if (seasonVideos.length === 0) return;

  let targetVideoId = item.state ? item.state.video_id : null;
  let tOffset = 1;
  let dur = 0;

  if (remove) {
    targetVideoId = seasonVideos[0].id;
    tOffset = 0;
    dur = 1;
  } else {
    const lastEpIdx = sorted.findIndex(v => v.id === seasonVideos[seasonVideos.length - 1].id);
    if (lastEpIdx !== -1 && lastEpIdx + 1 < sorted.length) {
      targetVideoId = sorted[lastEpIdx + 1].id;
      tOffset = 0;
      dur = 0;
    } else {
      targetVideoId = seasonVideos[seasonVideos.length - 1].id;
      tOffset = 1;
      dur = 1;
    }
  }

  let watchedSet = new Set();
  if (item.state && typeof item.state.watched === 'string') {
    watchedSet = parseWatchedBitfield(item.state.watched, allVideos);
  } else if (item.state && (item.state.timesWatched || 0) > 0) {
    allVideos.forEach(v => watchedSet.add(v));
  }

  if (remove) {
    seasonVideos.forEach(v => watchedSet.delete(v.id));
  } else {
    seasonVideos.forEach(v => watchedSet.add(v.id));
  }

  await saveWatchedState(item, allVideos, watchedSet, targetVideoId, tOffset, dur);
}

async function handleMarkWatched() {
  if (!state.currentDetail || !state.auth) return;
  const { libraryItem } = state.currentDetail;
  const btn = $('btn-watched');
  btn.disabled = true;
  try {
    const result = await StremioAPI.markAsWatched(state.auth.authKey, libraryItem);
    if (result.ok) {
      if (!libraryItem.state) libraryItem.state = {};
      const newState = result.isWatched ? 1 : 0;
      btn.classList.remove('loading');
      if (newState === 1) {
        btn.innerHTML = `
          <div class="icon-circle" style="background: rgba(16, 185, 129, 0.2); color: #10b981;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <span style="color: #10b981;" data-i18n="marked_watched">${I18N[state.language || 'ar'].marked_watched || 'شُوهد'}</span>
        `;
        libraryItem.state.timesWatched = 1;
        showToast(I18N[state.language || 'ar'].msg_marked_watched, 'success');
      } else {
        btn.innerHTML = `
          <div class="icon-circle">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
          </div>
          <span data-i18n="mark_watched">${I18N[state.language || 'ar'].mark_watched || 'تحديد كمشاهد'}</span>
        `;
        libraryItem.state.timesWatched = 0;
        showToast(I18N[state.language || 'ar'].msg_removed_watched, 'info');
      }
      await Library.invalidate();
    } else {
      showToast(I18N[state.language || 'ar'].msg_error, 'error');
    }
  } catch (e) {
    console.error('Mark watched failed:', e);
    showToast(I18N[state.language || 'ar'].msg_error, 'error');
  } finally {
    btn.disabled = false;
  }
}

async function handleRemove() {
  if (!state.currentDetail || !state.auth) return;
  const { libraryItem } = state.currentDetail;
  const btn = $('btn-remove');
  btn.disabled = true;
  try {
    await StremioAPI.removeFromLibrary(state.auth.authKey, libraryItem);
    showToast(I18N[state.language || 'ar'].msg_removed_library, 'info');
    await Library.invalidate();
    goBack();
    await loadLibrary(true);
  } catch (e) {
    console.error('Remove failed:', e);
    showToast(I18N[state.language || 'ar'].msg_remove_failed, 'error');
    btn.disabled = false;
  }
}

// ==================== Navigation ====================
function goBack() {
  const screenDetail = $('screen-detail');

  if (state.detailMode === 'card') {
    screenDetail.classList.add('card-fade-out');
    setTimeout(() => {
      screenDetail.classList.add('hidden');
      screenDetail.classList.remove('show-modal', 'card-fade-out');
      state.currentDetail = null;
      if (state.isFromSearch) {
        const searchModal = $('search-modal');
        if (searchModal) searchModal.classList.remove('hidden');
      }
    }, 250);
    return; // Early return since state clearing is handled in timeout
  } else {
    showScreen('main');
  }

  state.currentDetail = null;
  if (state.isFromSearch) {
    const searchModal = $('search-modal');
    if (searchModal) searchModal.classList.remove('hidden');
  }
}

// ==================== Pending Search ====================
async function checkPendingSearch() {
  try {
    const { pendingSearch } = await chrome.storage.session.get(['pendingSearch']);
    if (pendingSearch) {
      await chrome.storage.session.remove(['pendingSearch']);
      toggleSearch();
      $('global-search-input').value = pendingSearch;
      await performSearch(pendingSearch);
    }
  } catch (e) {
    console.warn('Pending search check failed:', e);
  }
}

// ==================== Helpers ====================
function escapeHTML(str) {
  if (!str) return '';
  const div = document.createElement('div');
  div.textContent = str;
  return div.innerHTML;
}

function debounce(fn, delay) {
  let timer;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

// ==================== Notifications & CW ====================
function countNewEpisodes(item) {
  if (item.type !== 'series') return 0;
  if (!state.stremioNotifications) return 0;
  const total = state.stremioNotifications.items?.[item._id] || 0;
  if (total === 0) return 0;
  const dismissedAt = state.dismissedNotifs[item._id];
  if (!dismissedAt) return total;
  const videos = state.stremioNotifications.videos?.[item._id];
  if (!videos) return total;
  return videos.filter(r => r > dismissedAt).length;
}

function dismissNotification(item, card) {
  const videos = state.stremioNotifications?.videos?.[item._id];
  const latestReleased = videos && videos.length > 0 ? Math.max(...videos) : Date.now();
  state.dismissedNotifs[item._id] = latestReleased;
  chrome.storage.local.set({ dismissedNotifs: state.dismissedNotifs });

  if (state.auth && state.auth.authKey) {
    StremioAPI.clearProgress(state.auth.authKey, item).catch(console.error);
  }

  if (card) {
    card.style.opacity = '0';
    setTimeout(() => {
      if (card.parentElement) card.remove();
    }, 300);
  }
}

async function fetchNotifications(libraryItems) {
  const seriesIds = libraryItems
    .filter((i) => i.type === 'series' && !i.removed && /^tt\d+$/.test(i._id))
    .map((i) => i._id);
  if (seriesIds.length === 0) {
    state.stremioNotifications = {};
    return;
  }
  const BATCH = 50;
  const batches = [];
  for (let i = 0; i < seriesIds.length; i += BATCH) batches.push(seriesIds.slice(i, i + BATCH));
  const itemMap = {};
  libraryItems.forEach((i) => { itemMap[i._id] = i; });

  const results = await Promise.all(batches.map(batch =>
    fetch(`https://v3-cinemeta.strem.io/catalog/series/last-videos/lastVideosIds=${batch.join(',')}.json`).then(r => r.json()).catch(() => ({ metasDetailed: [] }))
  ));

  const notifications = {};
  const notificationVideos = {};
  const now = new Date();

  results.forEach(result => {
    (result.metasDetailed || []).forEach(meta => {
      const item = itemMap[meta.id || meta.imdb_id];
      if (!item) return;
      const lastWatched = item.state?.lastWatched ? new Date(item.state.lastWatched) : null;
      const ctime = item._ctime ? new Date(item._ctime) : null;
      const baseline = (lastWatched && ctime && lastWatched > ctime) ? lastWatched : ctime;
      if (!baseline) return;

      const newVideos = (meta.videos || []).filter(v => {
        if (!v.released) return false;
        const released = new Date(v.released);
        if (released > now) return false;
        if (released <= baseline) return false;
        return true;
      });
      if (newVideos.length > 0) {
        notifications[meta.id || meta.imdb_id] = newVideos.length;
        notificationVideos[meta.id || meta.imdb_id] = newVideos.map(v => new Date(v.released).getTime());
      }
    });
  });

  state.stremioNotifications = { items: notifications, videos: notificationVideos, fetchedAt: Date.now() };
}



function updateBackBtnIcon() {
  const btn = $('back-btn');
  if (!btn) return;

  if (state.detailMode === 'card') {
    btn.innerHTML = `
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    `;
    btn.removeAttribute('style');
  } else {
    btn.innerHTML = `
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="m9 18 6-6-6-6" />
      </svg>
    `;
    btn.removeAttribute('style');
  }
}
